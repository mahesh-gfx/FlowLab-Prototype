{
  "name": "data-viz-tool",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/nodes",
    "packages/backend",
    "packages/frontend"
  ],
  "scripts": {
    "build": "lerna run build",
    "start": "lerna run start",
    "test": "lerna run test",
    "dev": "concurrently --names \"frontend,backend\" --prefix-colors \"blue,green\" \"npm:start:frontend\" \"npm:start:backend\""
  },
  "devDependencies": {
    "concurrently": "^6.2.0",
    "lerna": "^4.0.0"
  }
}
